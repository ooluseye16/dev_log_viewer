name: dev_log_client_example
description: Example Flutter app demonstrating dev_log_client integration.
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  dev_log_client:
    path: ../
  dio: ^5.0.0

dev_dependencies:
  flutter_lints: ^6.0.0
